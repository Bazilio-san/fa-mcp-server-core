# Token Generator

A web interface for creating and verifying JWT tokens.

Allows you to generate tokens with a configurable lifetime and additional parameters, as well as check their validity.

## Launch
```bash
npm run token-gen
```

Open: http://localhost:3030
